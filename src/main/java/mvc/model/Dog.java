package mvc.model;

public class Dog extends Pet {

	/**
	 * Constructor for the Dog subclass of Pet.
	 */
	public Dog(int id, String name, String type, String species, int age, boolean adopted) {
		super(id, name, type, species, age, adopted);
	}
		
}
